# ===============================================
# 🔒 安全配置文件示例
# ⚠️ 重要：复制此文件为 .env 并替换所有示例值
# ⚠️ 不要在生产环境使用这些示例值！
# ===============================================

# Database - MySQL配置
DATABASE_URL="mysql://shuaidi:e123456%40@159.75.174.234:3306/interview_coder"

# Redis配置
REDIS_URL="redis://localhost:6379"
REDIS_PASSWORD=""
REDIS_DB=0

# 🔒 JWT密钥配置 - 必须使用强随机密钥！
# 生成方法: node -e "console.log(require('crypto').randomBytes(64).toString('base64'))"
JWT_SECRET="REPLACE_WITH_STRONG_RANDOM_64_BYTE_BASE64_KEY"
JWT_REFRESH_SECRET="REPLACE_WITH_DIFFERENT_STRONG_RANDOM_64_BYTE_BASE64_KEY"
JWT_EXPIRES_IN="1h"
JWT_REFRESH_EXPIRES_IN="7d"

# 🔒 邮件配置 - 用于发送验证码
SMTP_HOST="smtp.gmail.com"
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER="your-secure-email@example.com"
SMTP_PASS="your-secure-app-password-here"
EMAIL_FROM="your-secure-email@example.com"

# Server Configuration
PORT=3001
NODE_ENV=development
CORS_ORIGIN="http://localhost:3000,http://localhost:54321"

# 🔒 AI API Keys - 替换为真实的API密钥
OPENAI_API_KEY="your-real-openai-api-key-here"
ANTHROPIC_API_KEY="your-real-anthropic-api-key-here"
GOOGLE_API_KEY="your-real-google-api-key-here"

# Ismaque API (as fallback) - 替换为真实密钥
ISMAQUE_API_KEY="your-real-ismaque-api-key-here"
ISMAQUE_BASE_URL="https://ismaque.org/v1"

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# WebSocket
WS_PORT=3002

# 验证码配置
VERIFICATION_CODE_EXPIRES_MINUTES=5
VERIFICATION_CODE_LENGTH=6

# 🔒 生产环境额外安全配置
COOKIE_SECURE=true
SESSION_SECRET="REPLACE_WITH_LONG_RANDOM_SESSION_SECRET" 